// AUTO-GENERATED — substituted during scaffolding.
// Defaults keep `tsc --noEmit` green on the foundation package itself;
// scaffold replaces them with the video's fps/width/height.
export const FPS = 30;
export const WIDTH = 1920;
export const HEIGHT = 1080;
// Scaffold markers (replaced by pipeline.py cmd_new):
// {{FPS}} {{WIDTH}} {{HEIGHT}}
